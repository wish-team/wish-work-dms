import React from 'react';
import PropTypes from 'prop-types';
import logo from '../assets/logo.svg';

export const NavigationBar = ({isLogin, logo}) => {
  let logo = logo;
    return(
      <div>

      </div>
  )
};

NavigationBar.propTypes = {

};
